// currently using the Native Next.js App Router sitemap generation, so this file is not needed. Keeping it here for reference if we decide to switch to next-sitemap in the future.
// "postbuild": "next-sitemap", is the script in package.json that triggers next-sitemap after the build process. If we decide to use next-sitemap, we will add this line.


// /** @type {import('next-sitemap').IConfig} */
// module.exports = {
//   siteUrl: "https://www.shutkiz.com", // 🔗 change this to your domain
//   generateRobotsTxt: true, // ✅ also creates robots.txt
//   sitemapSize: 7000, // optional: splits large sitemaps
//   exclude: ["/admin/*", "/api/*"], // optional: exclude routes
// };
